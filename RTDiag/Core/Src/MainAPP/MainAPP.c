#include <stdio.h>
#include "MainAPP/MainAPP.h"




/* main() {
    State currentState = STATE_INITIAL;
    Event event;

    event = EVENT_START;
    currentState = handleEvent(currentState, event);

    event = EVENT_TRIGGER1;
    currentState = handleEvent(currentState, event);

    event = EVENT_TRIGGER2;
    currentState = handleEvent(currentState, event);

    event = EVENT_END;
    currentState = handleEvent(currentState, event);

    return 0;
}*/
// Function to handle state transitions
State handleEvent(State currentState, Event event)
{ State nextState = currentState;

// structure of switch case pour determine le currentstate enswite autre siwtch case pour le event
return nextState  ;
}

